{
  "name": "service-2-backend",
  "version": "1.0.0",
  "description": "Service 2 Backend — Module 1 (Analytics) & Module 2 (Events)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev":   "nodemon server.js",
    "test":  "node -e \"require('./server'); console.log('✅ server loads OK');\""
  },
  "dependencies": {
    "cors":    "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
